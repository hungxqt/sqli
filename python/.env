# Django Environment Variables for Docker (Host MySQL)
DEBUG_MODE=true
DB_HOST=host.docker.internal
DB_PORT=3306
DB_NAME=django
DB_USER=root
DB_PASSWORD=

# Security (use only for development)
DJANGO_SECRET_KEY=django-insecure-it-s^0!sy7cpdf&falykep!#o8cdhrjuh^z(ma#(tl_ko-_6^s

# Debug settings
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# Optional: Override host for specific environments
# DB_HOST=192.168.1.100  # Use specific IP if host.docker.internal doesn't work
